%%
%% This is file `beamerthemekami.sty',
%% generated with the docstrip utility.
%%
%% The original source files were:
%%
%% beamerthemekami.dtx  (with options: `package')
%% ---------------------------------------------------------------------------
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{beamerthemekami}
  [2017/07/18 Kami Beamer theme]
\RequirePackage{etoolbox}
\RequirePackage{pgfopts}
\pgfkeys{/kami/.cd,
  .search also={
    /kami/inner,
    /kami/outer,
    /kami/color,
    /kami/font,
  }
}
\pgfkeys{
  /kami/titleformat plain/.cd,
    .is choice,
    regular/.code={%
      \let\kami@plaintitleformat\@empty%
      \setbeamerfont{standout}{shape=\normalfont}%
    },
    smallcaps/.code={%
      \let\kami@plaintitleformat\@empty%
      \setbeamerfont{standout}{shape=\scshape}%
    },
    allsmallcaps/.code={%
      \let\kami@plaintitleformat\MakeLowercase%
      \setbeamerfont{standout}{shape=\scshape}%
      \PackageWarning{beamerthemekami}{%
        Be aware that titleformat plain=allsmallcaps can lead to problems%
      }
    },
    allcaps/.code={%
      \let\kami@plaintitleformat\MakeUppercase%
      \setbeamerfont{standout}{shape=\normalfont}%
      \PackageWarning{beamerthemekami}{%
        Be aware that titleformat plain=allcaps can lead to problems%
      }
    },
}
\pgfkeys{
  /kami/titleformat/.code=\pgfkeysalso{
      font/titleformat title=#1,
      font/titleformat subtitle=#1,
      font/titleformat section=#1,
      font/titleformat frame=#1,
      titleformat plain=#1,
    }
}
\pgfkeys{/kami/.cd,
  usetitleprogressbar/.code=\pgfkeysalso{outer/progressbar=frametitle},
  noslidenumbers/.code=\pgfkeysalso{outer/numbering=none},
  usetotalslideindicator/.code=\pgfkeysalso{outer/numbering=fraction},
  nosectionslide/.code=\pgfkeysalso{inner/sectionpage=none},
  darkcolors/.code=\pgfkeysalso{color/background=dark},
  blockbg/.code=\pgfkeysalso{color/block=fill, inner/block=fill},
}
\newcommand{\kami@setdefaults}{
  \pgfkeys{/kami/.cd,
    titleformat plain=regular,
  }
}
\useinnertheme{kami}
\useoutertheme{kami}
\usecolortheme{kami}
\usefonttheme{kami}
\AtEndPreamble{%
  \@ifpackageloaded{pgfplots}{%
    \RequirePackage{pgfplotsthemetol}
  }{}
}
\newcommand{\metroset}[1]{\pgfkeys{/kami/.cd,#1}}
\def\kami@plaintitleformat#1{#1}
\newcommand{\plain}[2][]{%
  \PackageWarning{beamerthemekami}{%
    The syntax `\plain' may be deprecated in a future version of kami.
    Please use a frame with [standout] instead.
  }
  \begin{frame}[standout]{#1}
    \kami@plaintitleformat{#2}
  \end{frame}
}
\newcommand{\mreducelistspacing}{\vspace{-\topsep}}
\kami@setdefaults
\ProcessPgfOptions{/kami}
\endinput
%%
%% End of file `beamerthemekami.sty'.
